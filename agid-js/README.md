# agid-js

JavaScript SDK scaffold for AGID.

Canonical API (target):
- `encode(lat, lon)`
- `decode(id)`
- `quantize(lat, lon)`
- `dequantize(face, qx, qy)`

Reference implementation currently lives in `src/lib/agid.ts`.
Spec: `../agid-spec/README.md`
